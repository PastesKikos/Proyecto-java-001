public static double calcularTotal(int cantidad, double precioUnitario) {
    double subtotal = cantidad * precioUnitario;
    if (cantidad > 10) {
        return subtotal * 0.90; // Aplica un 10% de descuento
    }
    return subtotal;
}
